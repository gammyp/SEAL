server:
  port: 8888
spring:
  application:
    name: configuration-service
  cloud:
    config:
      server:
        git:
          uri: https://github.com/gammyp/SEAL-Configuration.git
          username: ${GIT_USERNAME}
          password: ${GIT_PASSWORD}